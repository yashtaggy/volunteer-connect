import React from 'react';

    const Home: React.FC = () => {
      return (
        <div>
          <h2>Welcome to Volunteer Connect!</h2>
          <p>Find events, register, and make a difference.</p>
          {/* We'll add navigation links here later */}
        </div>
      );
    };

    export default Home;