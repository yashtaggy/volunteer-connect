spring.application.name=backend
# --- Database Configuration (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/volunteer_connect
spring.datasource.username=postgres
spring.datasource.password=251198
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA/Hibernate Configuration ---
# Automatically creates or updates the database schema.
# Recommended for development, but be cautious in production!
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in the console
spring.jpa.show-sql=true

# Format the SQL queries for readability
spring.jpa.properties.hibernate.format_sql=true

# --- Server Port (Optional) ---
# You can specify the port your Spring Boot app runs on. Default is 8080.
server.port=8080

# --- JWT Configuration ---
jwt.secret=F1q0rCsR9Or0ov8EHGDTPSKfeoY8/E+HSPXZq8WfF3s=
jwt.expiration=36000000